apiVersion: v1
kind: Service
metadata:
  name: go-sample-app-svc
  namespace: production
spec:
  selector:
    app: sample-app
    env: production
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
